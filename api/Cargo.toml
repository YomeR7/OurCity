[package]
name = "api"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { workspace = true }
serde = { workspace = true }
uuid = { workspace = true }
axum = { workspace = true, optional = true }

[features]
default = []
server = ["dep:axum"]
